<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A minimal framework theme. All real styling happens in Compose
      (see ui/theme/Theme.kt); this only covers the window before the first
      frame is drawn.
    -->
    <style name="Theme.TickCount" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
